# NGM Tour Stats Website

The web UI is paused for this package version.

Use `ngm-stats.py` and the exported screenshots for the current workflow. The files in this folder are kept only so the online stats interface can be rebuilt later without starting over.
